version: 2

jobs:
  build:
    docker:
      - image: circleci/node:10.6.0-browsers
    steps:
      - checkout
      - run: yarn install
      - run: bash .env
      - run: npm test

# TODO: create Dockerfile for use in a step to build an image
#   (is this worth it or can we just use the build artifacts... TBD)