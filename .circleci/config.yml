version: 2

jobs:
  build:
    docker:
      - image: circleci/node:10.9.0-browsers
    steps:
      - checkout
      - run: yarn install
      - run: yarn test
      - run: IGNORE_KEYS=true yarn build $API_KEY